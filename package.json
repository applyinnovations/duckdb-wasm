{
    "repository": {
        "type": "git",
        "url": "https://github.com/applyinnovations/duckdb-wasm.git"
    },
    "private": true,
    "workspaces": [
        "packages/*",
        "examples/*"
    ],
    "scripts": {
        "lint:fix": "yarn workspace @applyinnovations/duckdb-wasm lint --fix && yarn workspace @applyinnovations/benchmarks lint --fix && yarn workspace @applyinnovations/duckdb-wasm-shell lint --fix",
        "docs": "yarn workspace @applyinnovations/duckdb-wasm docs",
        "benchmarks:build:browser": "yarn workspace @applyinnovations/benchmarks build:browser",
        "benchmarks:build:node": "yarn workspace @applyinnovations/benchmarks build:node",
        "benchmarks:build": "yarn workspace @applyinnovations/benchmarks build"
    },
    "devDependencies": {
        "badge-maker": "^3.3.1"
    }
}
